嵌入部分：
1.分块
    使用Matlab观察分块阈值
    Tstdev(分块标准差阈值)，blockSize(分块大小),
    分成ROI,RONI区域，得到：
    vector<BYTE> seq : 0 PE region, 1 HS region (分块序列)
    vector<vector<BYTE>> Iref: 0 PE region, 255 0 HS region (分块参照图)
2.RONI区域用HS方法嵌入
    vector<pair<int, int>> valuePair
3.ROI区域用PE方法嵌入
    T, TL, TR, TF,
4.得到Im, 计算分块记录码bookkeeping
    
提取部分：
1.分块
    由Tstdev, blockSize, bookkeeping得到seq’, 且seq’ == seq
2.ROI区域用PE方法提取
3.RONI区域用HS方法提取 
4.得到Ir, Ir == Io